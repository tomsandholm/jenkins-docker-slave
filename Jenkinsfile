pipeline {
    agent any
    stages {
        stage('shell environment') {
            steps {
                sh 'echo $PATH'
                sh 'pwd'
                sh 'id'
                sh 'ls -l'
            }
        }
    }
}
